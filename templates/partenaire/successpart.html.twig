{% extends 'partenaire_back.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
    <div class="container" style="margin-top: 200px">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <div class="card">
                    <h2>Welcome to your profile  {{ app.user.prenom }} {{ app.user.nom }}</h2>
                        {% if app.user %}
                        <ul>
                            <li>Your role is : {{ app.user.type }}</li>
                        </ul>
                    {% endif %}    
                </div>
                <a href="{{ path('listlivreur') }}" class="btn btn-primary">History</a>
                <a href="{{ path('logout') }}" class="btn btn-primary">logout</a>
            </div>
        </div>
    </div>

{% endblock %}
